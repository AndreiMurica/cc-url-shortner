apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-shortener-app.fullname" . }}
  labels:
    {{- include "url-shortener-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "url-shortener-app.selectorLabels" . | nindent 4 }}
