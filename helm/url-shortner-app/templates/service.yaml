apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-shortner-app.fullname" . }}
  labels:
{{ include "url-shortner-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
{{ include "url-shortner-app.selectorLabels" . | nindent 4 }}
